#include <stdio.h>

/**
* main - causes of infinite loop
* Return: (0)
*/

int main(void)
{
printf('infinite loop incoming :(\n')
i = 0;
	 
	 * i = 0;
	 * while (i < 10)
	 *{
	 * putchar(i);
	 *}
	 *
printf ('infinite loop avoided :\\0/n');
return (0);
}
